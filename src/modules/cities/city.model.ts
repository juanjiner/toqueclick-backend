export interface Departament {
    id: string;
    departament: string;
}

export interface Province {
    id: string;
    province: string;
}

export interface District {
    id: string;
    district: string;
}